// this should be consistent with common/styles/_base.scss

// namespace of this style, so the naming won't collide to the
// developer's own styles.
export const ns = 'hephaestus';

export const elevated = `${ns}-elevated`;
export const outlined = `${ns}-outlined`;
export const container = `${ns}-container`;

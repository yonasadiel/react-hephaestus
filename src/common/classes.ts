/**
 * Created to easily manage the class names.
 * This file should be in sync with ./_classes.scss .
 */

// Namespace of the package, so that developers wont't
// have class name conflicts.
export const NS = 'hephaestus';

/*** General ***/
export const ELEVATED = `${NS}-elevated`;
export const OUTLINED = `${NS}-outlined`;

/*** Component ***/
export const BUTTON = `${NS}-button`;
export const CARD = `${NS}-card`;
export const TEXT_INPUT = `${NS}-input-text`;

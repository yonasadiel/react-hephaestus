import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';

import '../../../common/storybook.css';
import Button from './Button';

<Meta
  title="core/Button"
  component={Button}
 />

# Button

<Preview>
  <Story name="Button">
    <div>
      <Button buttonType="contained">Contained</Button> <divider />
      <Button buttonType="outlined">Outlined</Button> <divider />
      <Button buttonType="text">Text</Button>
    </div>
  </Story>
</Preview>

## Props

This component accepts all button props, with additional props:

| Prop names              | Description                                              | Default                  |
| ----------------------- | -------------------------------------------------------- | ------------------------ |
| <code>buttonType</code> | <code>'contained' &#124; 'outlined' &#124; 'text'</code> | <code>'contained'</code> |
